# Telegram
BOT_TOKEN=<token>

# Database
POSTGRES_USER=<user>
POSTGRES_PASSWORD=<psswd>
POSTGRES_DB=<db>
POSTGRES_HOST=<host>
POSTGRES_PORT=<port>

# SQLAlchemy URLs
DATABASE_URL=<url>

# App environment
ENV=prod

# Начальное заполнение таблицы users при миграции (один раз).
# После миграции пользователями управляют через панель управления (/users).
ALLOWED_USER_IDS=[<user_id_1>,<user_id_2>]

# Web app (VkusVill import)
WEB_BASE_URL=https://your-domain.com
WEB_PORT=8000
WEB_PASSWORD=<password>
WEB_ROOT_PATH=/bot