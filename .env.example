# Telegram
BOT_TOKEN=<token>

# Database
POSTGRES_USER=<user>
POSTGRES_PASSWORD=<psswd>
POSTGRES_DB=<db>
POSTGRES_HOST=<host>
POSTGRES_PORT=<port>

# SQLAlchemy URLs
DATABASE_URL=<url>

# App environment
ENV=prod

# Telegram ID администратора.
# При миграции создаётся пользователь с этим ID и ролью admin.
# При входе в панель роль автоматически повышается до admin.
ADMIN_TELEGRAM_ID=<telegram_id>

# Web app (VkusVill import)
WEB_BASE_URL=https://your-domain.com
WEB_PORT=8000
WEB_PASSWORD=<password>
WEB_ROOT_PATH=/bot