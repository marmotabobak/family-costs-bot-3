# Family Costs Bot

[![CI/CD](https://github.com/marmotabobak/family-costs-bot-3/workflows/CI%2FCD/badge.svg)](https://github.com/marmotabobak/family-costs-bot-3/actions)
[![codecov](https://codecov.io/gh/marmotabobak/family-costs-bot-3/branch/master/graph/badge.svg)](https://codecov.io/gh/marmotabobak/family-costs-bot-3)
[![Python 3.10+](https://img.shields.io/badge/python-3.10%20%7C%203.11%20%7C%203.12-blue)](https://www.python.org/)
[![Code style: ruff](https://img.shields.io/badge/code%20style-ruff-000000.svg)](https://github.com/astral-sh/ruff)

Telegram-бот для учёта расходов.

## Функциональность

Telegram-бот для учёта семейных расходов с поддержкой нескольких пользователей и просмотра статистики.

**Основные возможности:**
- Пользователи отправляют сообщения с расходами
- Бот парсит сообщения и сохраняет расходы в БД (PostgreSQL)
- Бот готовит сводные данные по внесенным расходам по запросу пользователей
- Web UI для импорта чеков из ВкусВилл
- Панель управления: расходы (CRUD), пользователи (CRUD), логи
- Контроль доступа к боту через таблицу users в БД

### Основные возможности

#### 📝 Ввод расходов
- **Многострочный ввод**: можно отправить несколько расходов одним сообщением
- **Поддержка отрицательных сумм**: для корректировок (например, `корректировка -500`)
- **Гибкий формат сумм**: поддерживаются целые и дробные числа с разделителями `.` или `,`
- **Автоматическое сохранение**: при успешном парсинге расходы сохраняются сразу
- **Подтверждение**: если часть строк не распарсилась, бот запрашивает подтверждение перед сохранением валидных строк

#### 📊 Просмотр расходов
- **Меню расходов**: команда `/menu` открывает интерактивное меню
- **Просмотр по периодам**:
  - Этот месяц
  - Прошлый месяц
  - Другие месяцы (выбор из списка доступных)
- **Просмотр по пользователям**: видно расходы всех пользователей бота
- **Детальный отчёт**: для каждого месяца показывается:
  - Общая сумма расходов
  - Список всех расходов с датами
  - Сортировка по дате
- **Форматирование сумм**: разделитель тысяч `_` в боте, неразрывный пробел в web; дробная часть `.00` не показывается

#### 🔒 Безопасность и доступ
- **Контроль доступа**: опциональный список разрешённых пользователей (`ALLOWED_USER_IDS`)
- **Изоляция данных**: каждый пользователь видит только свои расходы (кроме просмотра статистики других пользователей)

#### 🌐 Web UI для импорта чеков
- **Импорт из ВкусВилл**: Web-интерфейс для загрузки и парсинга чеков
- **Автоматическое добавление**: расходы из чеков автоматически добавляются в базу данных

#### 🌐 Панель управления (Admin Panel)
- **Расходы (CRUD)**: пагинированный список с настроимой страницей (по умолчанию 100), фильтрация (по названию, пользователю, дате, сумме), сортировка по любому полю, многовыбор с массовым удалением, изменением даты и пользователя, добавление, редактирование, удаление записей
- **Пользователи (CRUD)**: добавление, редактирование, удаление пользователей бота; контроль доступа через таблицу `users`
- **Логи**: раздел для будущего просмотра логов (placeholder)
- **Авторизация**: вход по паролю из переменной `WEB_PASSWORD`, сессия 24 часа
- **Безопасность**: CSRF-защита на все формы, rate-limiting на логин (5 попыток / 5 мин), secure-cookie в prod, выбор пользователя через dropdown из таблицы users

### Формат сообщения

```
расход сумма
расход сумма
...
```

**Правила:**
- Каждая строка содержит название расхода и сумму, разделённые пробелом
- Сумма: целое или вещественное число (разделитель `.` или `,`)
- Сумма может быть отрицательной (для корректировок)
- Пустые строки игнорируются
- Максимальная длина сообщения: 4096 символов
- Максимальная длина строки: 100 символов
- Максимальное количество строк: 100

**Примеры:**
```
Продукты 100
вода из Лавки 123.45
морковь 123,00
заказ из Озона №12345 234
корректировка расхода -500.24
```

### Команды

| Команда | Описание |
|---------|----------|
| `/start` | Приветствие и справка по формату сообщений |
| `/menu` | Открыть меню расходов |
| `/help` | Справка по формату сообщений и работе с ботом |

### Интерактивные кнопки

#### В меню расходов:
- **📊 Мои расходы** — просмотр своих расходов
- **👤 Расходы [user_id]** — просмотр расходов другого пользователя
- **📅 Этот месяц** — расходы за текущий месяц
- **📅 Прошлый месяц** — расходы за предыдущий месяц
- **📅 Другие месяцы** — выбор из списка доступных месяцев

### Примеры использования

#### Пример 1: Ввод нескольких расходов
```
Пользователь: Продукты 500
              Молоко 80
              Хлеб 50

Бот: ✅ Записано 3 расхода:
     • Продукты: 500
     • Молоко: 80
     • Хлеб: 50
```

#### Пример 2: Ввод с корректировкой
```
Пользователь: Продукты 1000
              Корректировка -100

Бот: ✅ Записано 2 расхода:
     • Продукты: 1000
     • Корректировка: -100
```

#### Пример 3: Просмотр расходов за месяц
```
Пользователь: /menu
Бот: 📋 Расходы:
     [📊 Мои расходы]
     [👤 Расходы 123456789]

Пользователь: [📊 Мои расходы]
Бот: 📊 Мои расходы
     [📅 Этот месяц]
     [📅 Прошлый месяц]
     [📅 Другие месяцы]

Пользователь: [📅 Этот месяц]
Бот: Январь 2026

     Всего: 1_630

     01: Продукты 500
     01: Молоко 80
     02: Хлеб 50
     15: Ресторан 1_000
```

## Технологии

- Python 3.12
- aiogram 3.x: Telegram Bot API
- FastAPI + Jinja2: Web UI для импорта чеков
- SQLAlchemy 2.x (async): ORM
- asyncpg: async драйвер PostgreSQL
- Alembic: миграции БД
- pytest: тестирование
- ruff + mypy: линтеры
- Docker: контейнеризация
- GitHub Actions: CI/CD

## Структура проекта

```
bot/
├── config.py              # Настройки (Pydantic Settings)
├── constants.py           # Константы сообщений бота
├── exceptions.py          # Кастомные исключения
├── main.py                # Точка входа
├── middleware.py          # Middleware контроля доступа (запрос users из БД)
├── utils.py               # Утилиты (format_amount, pluralize)
├── logging_config.py      # Настройка логирования
├── db/
│   ├── base.py            # Declarative Base
│   ├── models.py          # SQLAlchemy модели (Message, User)
│   ├── session.py         # async engine + sessionmaker
│   ├── dependencies.py    # Контекстный менеджер сессий
│   └── repositories/
│       ├── messages.py    # Репозиторий Message (CRUD операции)
│       └── users.py       # Репозиторий User (CRUD операции)
├── routers/
│   ├── common.py          # /start, /help handlers
│   ├── messages.py        # Обработчик сообщений с расходами
│   └── menu.py            # Меню расходов и просмотр статистики
├── services/
│   └── message_parser.py  # Парсинг сообщений
└── web/
    ├── app.py             # FastAPI приложение + import flow
    ├── auth.py            # Общая авторизация (сессии, login/logout, CSRF)
    ├── costs.py           # Роутер управления расходами (CRUD)
    ├── users.py           # Роутер управления пользователями (CRUD)
    ├── logs.py            # Роутер логов (placeholder)
    ├── static/            # CSS стили
    └── templates/         # Jinja2 шаблоны
        ├── costs/         # Шаблоны расходов + base.html (nav)
        ├── users/         # Шаблоны управления пользователями
        └── logs/          # Шаблоны логов

tests/
├── conftest.py            # Настройка окружения, safety checks
├── unit/                  # Unit-тесты (307 тестов)
│   ├── conftest.py        # Shared fixtures
│   └── test_*.py          # Тесты модулей
├── e2e/                   # E2E journey тесты (41 тест)
│   ├── TEST_PLAN.md       # План E2E тестирования
│   └── test_admin_e2e.py  # Полные сценарии работы панели
└── integration/           # Интеграционные тесты (требуют PostgreSQL)

migrations/                # Alembic миграции
```

- - - - - - - - - -


## Quick start (локальная разработка)

### 1. Настройка dev-окружения

```bash
# Клонирование репозитория
git clone <repo>
cd <FOLDER_NAME>

# Подготовка виртуального окружения
python -m venv venv
source venv/bin/activate

# Установка зависимостей
make install-dev

# Установка pre-commit хуков
make hooks
```

### 2. Конфигурация

Создайте `.env`:
```env
BOT_TOKEN=<TELEGRAM_BOT_TOKEN>

POSTGRES_USER=test
POSTGRES_PASSWORD=test
POSTGRES_DB=test_db
POSTGRES_PORT=5432

DATABASE_URL=postgresql+asyncpg://test:test@localhost:5432/test_db
ENV=dev

# Опционально: список разрешённых пользователей (через запятую)
# Если не указано или пусто - доступ открыт для всех
ALLOWED_USER_IDS=123456789,987654321

# Опционально: пароль для Web UI управления расходами
# Если не указано — доступ к /costs закрыт
WEB_PASSWORD=my_secret_password

# Опционально: префикс URL для web UI (nginx снимает перед проксированием в FastAPI)
# В dev оставить пустым
WEB_ROOT_PATH=
```

### 3. Запуск

```bash
make db        # Запустить БД (PostgreSQL) в контейнере
make migrate   # Применить миграции БД
make bot       # Запустить бота
```

### 4. Web UI (опционально)

```bash
# Запустить web-сервер
poetry run uvicorn bot.web.app:app --reload --port 8000
```

**Импорт чеков из ВкусВилл** (только в dev-режиме):
```
GET http://localhost:8000/dev/create-token/{user_id}
```

**Панель управления** (если задан `WEB_PASSWORD`):
```
GET http://localhost:8000/login
```

### Web API Endpoints

В dev-режиме (без `WEB_ROOT_PATH`) эндпоинты доступны по корню. В prod nginx проксирует `/bot/` → FastAPI, поэтому все URL доступны под префиксом `/bot` (например, `/bot/login`).

**Общие / авторизация:**

| Endpoint | Описание |
|----------|----------|
| `GET /` | Редирект на `/costs` |
| `GET /health` | Health check для мониторинга |
| `GET /login` | Страница входа в панель |
| `POST /login` | Авторизация по паролю |
| `GET /logout` | Выход из сессии |

**Расходы (требует авторизации):**

| Endpoint | Описание |
|----------|----------|
| `GET /costs` | Список расходов (с пагинацией) |
| `GET /costs/add` | Форма добавления расхода |
| `POST /costs/add` | Добавить новый расход |
| `GET /costs/{id}/edit` | Форма редактирования расхода |
| `POST /costs/{id}/edit` | Сохранить изменения расхода |
| `POST /costs/{id}/delete` | Удалить расход |
| `POST /costs/bulk-delete` | Массовое удаление расходов |
| `POST /costs/bulk-change-date` | Массовое изменение даты расходов |
| `POST /costs/bulk-change-user` | Массовое изменение пользователя расходов |

**Пользователи (требует авторизации):**

| Endpoint | Описание |
|----------|----------|
| `GET /users` | Список пользователей |
| `GET /users/add` | Форма добавления пользователя |
| `POST /users/add` | Добавить пользователя |
| `GET /users/{id}/edit` | Форма редактирования пользователя |
| `POST /users/{id}/edit` | Сохранить изменения пользователя |
| `POST /users/{id}/delete` | Удалить пользователя |

**Логи (требует авторизации):**

| Endpoint | Описание |
|----------|----------|
| `GET /logs` | Страница логов (placeholder) |

**Импорт чеков (токен-based, без авторизации):**

| Endpoint | Описание |
|----------|----------|
| `GET /dev/create-token/{user_id}` | Создать токен (только dev) |
| `GET /import/{token}` | Страница загрузки файла |
| `POST /import/{token}/upload` | Загрузить JSON с чеками |
| `GET /import/{token}/select` | Выбор позиций для сохранения |
| `POST /import/{token}/save` | Сохранить выбранные позиции |


- - - - - - - - - -


## Makefile

### DEV-окружение

| Команда            | Описание                               |
|--------------------|----------------------------------------|
| `make install`     | Установить PROD зависимости            |
| `make install-dev` | Установить DEV зависимости             |
| `make db`          | Запустить БД (PostgreSQL) в контейнере |
| `make bot`         | Запустить бота локально (для DEV)      |

### Docker

| Команда         | Описание                               |
|-----------------|----------------------------------------|
| `make up`       | Запустить все сервисы (postgres + bot) |
| `make down`     | Остановить контейнеры                  |
| `make logs`     | Логи всех контейнеров                  |
| `make logs-bot` | Логи бота                              |
| `make logs-db`  | Логи PostgreSQL                        |

### Миграции БД

| Команда                  | Описание            |
|--------------------------|---------------------|
| `make migrate`           | Применить миграции  |

**Качество кода:**

| Команда           | Описание                            |
|-------------------|-------------------------------------|
| `make lint`       | Запустить ruff + mypy               |
| `make hooks`      | Установить pre-commit хуки          |
| `make pre-commit` | Запустить pre-commit на всех файлах |
| `make test`       | Запустить тесты                     |
| `make cov`        | Тесты с coverage отчётом            |
| `make clean`      | Очистить кэши                       |

## Тестирование

Проект включает комплексное тестирование:
- **Unit-тесты**: 325 тестов покрывающих все модули
- **E2E тесты**: 50 тестов полных сценариев работы (auth, users CRUD, costs CRUD, import flow, security)
- **Интеграционные тесты**: 130 тестов для проверки взаимодействия с PostgreSQL (требуют запущенную БД)
- **Покрытие кода**: ~96%

```bash
# Запустить все тесты
make test

# Подготовить отчёт по тестовому покрытию
make cov

# Конкретный файл
pytest tests/unit/test_message_parser.py -v

# Только интеграционные тесты
pytest tests/integration/ -v
```

### Покрытие тестами

| Модуль | Покрытие | Статус |
|--------|----------|--------|
| `message_parser.py` | 100% | ✅ |
| `constants.py` | 100% | ✅ |
| `middleware.py` | 100% | ✅ |
| `routers/common.py` | 100% | ✅ |
| `routers/messages.py` | 84% | ✅ |
| `routers/menu.py` | 79% | ✅ |
| `config.py` | 98% | ✅ |
| `db/repositories/messages.py` | 90%+ | ✅ |
| `db/repositories/users.py` | 100% | ✅ |
| `web/auth.py` | 95%+ | ✅ |
| `web/costs.py` | 95%+ | ✅ |
| `web/users.py` | 95%+ | ✅ |
| `web/logs.py` | 100% | ✅ |

## Pre-commit хуки

Автоматически запускаются при коммите:
- `trailing-whitespace` — удаление пробелов
- `end-of-file-fixer` — newline в конце файлов
- `check-yaml` — валидация YAML
- `ruff` — линтинг + автоисправление
- `ruff-format` — форматирование
- `mypy` — проверка типов

## Docker

**Dockerfile** — образ на базе Python 3.12-slim

**docker-compose.yml:**
- `postgres` — PostgreSQL 14 с healthcheck
- `bot` — бот (ждёт готовности postgres)

```bash
# Запустить всё в Docker
make up

# Только PostgreSQL (для локальной разработки)
make db

# Просмотр логов
make logs        # все контейнеры
make logs-bot    # только бот
make logs-db     # только postgres
```

## CI/CD (GitHub Actions)

Push в master запрещён (только через PR).

**При push в ветку / PR:**
1. Линтинг (ruff, mypy) на Python 3.10, 3.11, 3.12
2. Тесты с ENV=test (с отчётом по покрытию в Codecov)

**При merge в master:**
3. Автоматический деплой на удалённый хост

### Настройка деплоя

Добавьте секреты в GitHub: Settings → Secrets and variables → Actions:

| Секрет           | Описание                  | Пример                  |
|------------------|---------------------------|-------------------------|
| `DEPLOY_HOST`    | IP или hostname сервера   | `192.168.1.100`         |
| `DEPLOY_USER`    | SSH пользователь          | `deploy`                |
| `DEPLOY_SSH_KEY` | Приватный SSH ключ        | `-----BEGIN OPENSSH...` |
| `DEPLOY_PORT`    | SSH порт (опционально)    | `22`                    |
| `DEPLOY_PATH`    | Путь к проекту на сервере | `/home/deploy/bot`      |

### Первоначальная настройка сервера

```bash
# Клонировать репозиторий
git clone <repo> /home/deploy/bot
cd /home/deploy/bot

# Создать .env с production значениями
cat > .env << EOF
BOT_TOKEN=your-production-token
POSTGRES_USER=prod_user
POSTGRES_PASSWORD=secure_password
POSTGRES_DB=family_costs
POSTGRES_PORT=5432
DATABASE_URL=postgresql+asyncpg://prod_user:secure_password@postgres:5432/family_costs
ENV=prod
# Начальное заполнение таблицы users при миграции (один раз).
# После миграции пользователями управляют через /users.
ALLOWED_USER_IDS=123456789,987654321
WEB_BASE_URL=https://your-domain.com
WEB_PASSWORD=secure_web_password
WEB_ROOT_PATH=/bot
EOF

# Запустить
docker-compose up -d --build
```

## Конфигурация

### Переменные окружения

| Переменная | Описание | Обязательная | Пример |
|------------|----------|--------------|--------|
| `BOT_TOKEN` | Токен Telegram бота | ✅ Да | `123456:ABC-DEF...` |
| `DATABASE_URL` | URL подключения к PostgreSQL | ✅ Да | `postgresql+asyncpg://user:pass@localhost:5432/db` |
| `ENV` | Режим работы (`dev`/`test`/`prod`) | ❌ Нет | `dev` |
| `ALLOWED_USER_IDS` | Начальное заполнение таблицы `users` при миграции (один раз); после этого пользователями управляют через `/users` | ❌ Нет | `123456789,987654321` |
| `WEB_BASE_URL` | Базовый URL web-сервера (для ссылок в боте) | ❌ Нет | `http://localhost:8000` |
| `WEB_PASSWORD` | Пароль для входа в Web UI управления расходами | ❌ Нет | `my_secret_password` |
| `WEB_ROOT_PATH` | Префикс URL для web UI (nginx снимает перед проксированием в FastAPI) | ❌ Нет | `/bot` |

### Переменная ENV

Переменная `ENV` управляет режимом работы:

| Значение | Описание                                    |
|----------|---------------------------------------------|
| `dev`    | Разработка: SQL-логирование включено        |
| `test`   | Тесты: автоматически устанавливается pytest |
| `prod`   | Production: SQL-логирование выключено       |

Тесты блокируются при `ENV=prod` для защиты production БД.

### Контроль доступа

Доступ к боту контролируется через таблицу `users` в БД. Middleware при каждом сообщении запрашивает список `telegram_id` из таблицы и отклоняет неизвестных пользователей.

**Начальное заполнение:** при первой миграции таблица `users` заполняется из переменной `ALLOWED_USER_IDS` (если задана). После этого пользователями управляют через панель управления (`/users`), а `ALLOWED_USER_IDS` больше не используется.

Если при миграции `ALLOWED_USER_IDS` не задана — таблица `users` остаётся пустой и доступ к боту закрыт для всех.

## Особенности реализации

### Обработка ошибок
- Валидация формата сообщений перед сохранением
- Проверка ограничений (длина сообщения, строки, количество строк)
- Обработка ошибок базы данных с откатом транзакций
- HTML-экранирование пользовательского ввода для безопасности

### Производительность
- Асинхронная работа с базой данных (asyncpg)
- Пул соединений для эффективного использования ресурсов
- Graceful shutdown при получении сигналов SIGTERM/SIGINT

### Безопасность
- Контроль доступа через middleware
- Изоляция данных пользователей
- Защита от SQL-инъекций через SQLAlchemy ORM
- Валидация входных данных

## TODO
- добавить id бота в модель данных расходов, чтобы разделить данные от пользователя из разных ботов
- собирать URL сервисом самостоятельно (не импортом из env): данные доступны из других env
- тестовый стенд с тестовым ботом
- тесты на bot.exceptions
- логирование в ENV=prod
- ревью текстов (например, указывать месяц при записи)

## BUGS
- попытка записи в БД расхода от неавторизованного пользователя
- запись расхода со спецсимволами, например: "тест_2 1" - записывает в БД, но потом ошибка отображения
  TelegramBadRequest: Telegram server says - Bad Request: can't parse entities: Can't find end of the entity starting at byte offset 304

## Лицензия

MIT
